runs:
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: full
    head: cbca585
    findings:
      - id: 1
        file: review-triage/tools/triagecheck/record.go
        line: 716
        summary: >-
          fix-derived の根拠の prior に「直前の回の採択を指す文字列」(指摘 3 など) を書くと
          検査が必ず落ちる。recurrence-detection.md の「発火したときに記録に書くもの」は
          prior の 3 つの形を condition で場合分けせずに定めているのに、検査は fix-derived の
          prior を「比べた回の plans の問題の識別子」か「捉え直し」だけに絞っている
        category: plugin-code
        audience: operator
        consequence:
          condition: review-triage が正本どおりに fix-derived の prior を「指摘 N」と書いたとき (直前の回の修正を review-triage-fix を経ずに人間が直したために plans が無い、または採択そのものを比べた先にした場合)
          who: operator
          what: >-
            検査が赤くなり、正本どおりに書いた記録が通らない。書き手は通すために prior を
            実在する問題の識別子に書き換える方向へ誘導され、根拠の監査経路 (なぜ同じ型と
            読んだか) が実態と食い違う
          detectability: 検査が赤くなるので落ちたことには気づく。ただし、書き換えて通した記録が実態と食い違うことは気づかない (文書と検査の食い違いを捕まえる関門は無い)
        premise_check:
          stages: A+B
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: record.go:713-718 を読み、fix-derived の prior を plansByRun の問題の
          識別子か、比べた回が reframed のときの「捉え直し」に限っていることを確認した。
          段 B: recurrence-detection.md の「発火したときに記録に書くもの」を逐語で読み、
          prior の 3 つの形 (問題の識別子・採択を指す文字列・捉え直し) が condition で
          場合分けされていないことを確認した。record-schema.md の prior の行も 3 値を
          並べ、どれを書くかを recurrence-detection.md に委ねている。文書と検査の契約が
          食い違うという主張は成り立つ。全 4 ゲートを評価して発火 0 件 — hypothetical:
          レビュアのプローブがそのままテストになる。developer-domain: 検査は利用者の
          記録にかかる。disproportionate-cost: 直すのは文の場合分け 1 文か検査の条件 1 行。
          already-visible: fix-derived で「指摘 N」を通すテストは無い
          (TestReviewTriageRecordRecurrenceViolations は不在の問題識別子と捉え直しの
          誤用だけ)。免除条項は検出能力の欠落の主張ではないので対象外。
          修正方法は設計判断を含む — 文書側を「fix-derived は問題の識別子か捉え直し、
          same-location は採択を指す文字列」と場合分けして絞るか、検査側で採択を指す
          文字列も受けるか。どちらにするかは review-triage-fix が人間に諮る対象
        attrs:
          severity: P2
          confidence: high
      - id: 2
        file: CONCEPTS.md
        line: 89
        summary: >-
          CONCEPTS.md の「修正由来の指摘」が「2 回続けて現れたら…次の修正の前に捉え直しを
          行う」と無条件に定めるが、手順の正本 reframing.md では人間が繰り返しを認めなければ
          捉え直しはせず declined で終える。用語の正本と手順の正本が食い違う
        category: doc-dev
        audience_initial: developer
        audience: operator
        consequence:
          condition: スキル文書が用語の正本として案内する CONCEPTS.md を先に読んだ実行者 (スキルを走らせるエージェントと、それを見ている人間) が、第 1 段で人間が「繰り返しではない」と答えた場面に当たったとき
          who: operator
          what: >-
            「2 回続いたら捉え直しを行う」を定義の一部と読み、declined で終える選択肢を
            持たないまま第 2 段へ進む。人間の判断を手順が上書きする形になり、俯瞰が
            人間の確認を軸にする設計 (問いが先、見立ては後) と食い違う
          detectability: 気づかない — 用語集と手順書の文の矛盾を捕まえる機械検査は無く、リンク検査は文面を見ない
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。被害者はパスの分類 (doc-dev) では開発者だが、review-triage-fix/SKILL.md と
          reframing.md が CONCEPTS.md を「用語の正本」として利用者 (スキルの実行者) に
          読ませるので、要求水準の高い側 (利用者) へ上書きした。段 A: CONCEPTS.md:89 と
          reframing.md:42 を逐語で読み比べ、前者が無条件の「捉え直しを行う」、後者が
          「認めなければ捉え直しは書かない」であることを確認した。
          全 4 ゲートを評価して発火 0 件 — hypothetical: 読めば再現する。
          developer-domain: 対象は利用者に届く文書。disproportionate-cost: 修正は 1 文の
          書き換え (「次の修正の前に俯瞰を行い、捉え直すかどうかを人間と決める」)。
          already-visible: 文の矛盾を検出する関門は無い。免除条項は対象外
        attrs:
          severity: P3
          confidence: high
    plans:
      - problem_id: P1
        cause: >-
          検査に fix-derived の prior と plans の照合を足したとき、prior の形を定める正本
          (recurrence-detection.md「発火したときに記録に書くもの」) を condition ごとの
          場合分けに直さず、検査側にだけ条件を入れた。文書は 3 つの形を無条件に並べたまま
          になり、文書と検査の契約が食い違った
        finding_ids: [1]
        approach: >-
          文書と検査のどちらを規範にするかは設計判断なので人間に返した (options)。
          人間は (a) を選んだ。文書を場合分けして絞り、検査は現状のまま。
          決まった側に合わせて、prior の形を定める 3 箇所 (recurrence-detection.md:38・
          record-schema.md:120 の prior の行・reframing.md 第 1 段の辺の説明) と検査
          (record.go の fix-derived の照合とそのテスト) を同じ動機で直す。順序は P2 と
          独立 (依存なし)
        investigation:
          scope: >-
            前向き: grep -rn '指摘 3\|採択を指す\|捉え直しを指す' CONCEPTS.md
            review-triage/README.md review-triage/skills (--include='*.md') → 一致は
            recurrence-detection.md:30,38 と record-schema.md:120 の 3 行。同じ語を
            持たない近くの辺として reframing.md「第 1 段 > 図」の辺の説明 (問題の識別子、
            採択、または捉え直し) を目で読んだ。後ろ向き: record.go:706-718 の照合と
            record_test.go の TestReviewTriageRecordRecurrenceViolations (prior が plans に
            無い・捉え直しの誤用の 2 ケース) と TestReviewTriageRecordRecurrencePriorReframed
          included:
            - "record-schema.md:120 の prior の行 — 3 値を並べて recurrence-detection.md に委ねており、正本を場合分けすれば追随が要る"
            - "reframing.md 第 1 段の辺の説明 — 同じ 3 値を並べている"
            - "record.go の fix-derived の照合と record_test.go の該当テスト — 検査側を規範にするなら不変、文書側を規範にするなら採択を指す文字列の受け入れを足す"
          excluded:
            - "recurrence-detection.md:30 (捉え直し後の照らし方) — prior に捉え直しを指す旨は場合分けと矛盾しないので変えない"
        options: >-
          (a) 文書を場合分けして絞る — fix-derived の prior は比べた回の問題の識別子か
          (比べた回が reframed なら)「捉え直し」、same-location の prior は採択を指す文字列
          (「指摘 3」)。検査は現状のまま。利点: fix-derived の根拠が必ず修正計画か捉え直しを
          指し、監査経路が最も強い。欠点: review-triage-fix を経ずに人間が直した回 (plans が
          無い) では fix-derived を書けず、その場合の扱い (補助の条件で書く、または plans を
          後から書く) を文書に足す必要がある。
          (b) 検査を緩める — fix-derived の prior に採択を指す文字列も許し、比べた回の
          findings の id と照合する。利点: 文書どおりの 3 形がどの condition でも通り、plans の
          無い回でも根拠を書ける。欠点: 「原因が直前の回の修正作業にある」という修正由来の
          定義に対し、修正計画ではなく採択を根拠にした検知が通り、監査経路が一段弱くなる。
          検査の分岐とテストが増える
        sha: 061f26b
        status: done
      - problem_id: P2
        cause: >-
          CONCEPTS.md の「修正由来の指摘」を要件の段階で書き、後で手順 (reframing.md) に
          「人間が繰り返しと認めなければ捉え直さない」を定めたとき、用語集の文を追随させ
          なかった
        finding_ids: [2]
        approach: >-
          CONCEPTS.md:89 の「次の修正の前に捉え直しを行う」を、俯瞰で人間と確かめ、繰り返しと
          認めたときに捉え直す、という条件つきの文に書き換える。用語の定義は変えない。
          P1 と依存なし。影響範囲が狭いので先に直す
        investigation:
          scope: >-
            前向き: grep -rn '捉え直しを行う\|捉え直します\|次の修正の前に' CONCEPTS.md
            review-triage/README.md review-triage/skills (--include='*.md') → CONCEPTS.md:89、
            README.md:16、recurrence-detection.md:57 の 3 行。後ろ向き: CONCEPTS.md を
            用語の正本として参照する reframing.md:3・recurrence-detection.md:3・
            review-triage-fix/SKILL.md 前提知識・grouping.md「捉え直しがあるとき」を目で読んだ
          excluded:
            - "README.md:16 — 「人間と確かめてから」と条件を含む描写で、無条件ではない"
            - "recurrence-detection.md:57 — 従来の目安の引用で、表を書く作業を俯瞰に委ねる文。無条件の捉え直しを述べていない"
            - "CONCEPTS.md の参照元 4 箇所 — 用語の定義そのものは変えないので追随は不要"
        sha: 1fc8b17
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 全量) で、報告のみで走った
      (実行前後で HEAD cbca585 と作業ツリーが不変であることを確認)。
      residual の 3 件のうち、記録に残す価値があるのは次の 2 つ。
      (1) reframing.md「対象の選び方」の「declined_reason も reframe も無い回」は、検査が
      detected にその 2 つを禁じているので後半の条件は常に真で、読み手には冗長。
      (2) same-location の prior は自由文字列のままで、fix-derived との非対称は監査経路の
      強さの違いとして残る (record-schema.md の設計どおり)。
      このブランチで足した検知の手順は、回 1 では判断しない (比べる過去の回が無い)。
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: 3e548d4
    findings:
      - id: 1
        file: review-triage/skills/review-triage/references/recurrence-detection.md
        line: 39
        summary: >-
          回 1 の修正で足した「直前の回に plans が無いときは修正由来の根拠は書けない」が
          無条件に書かれているが、直前の回が捉え直し済み (reframed) なら plans が無くても
          prior: 捉え直し で修正由来の根拠を書けて検査も通る。同じ文書の捉え直し後の照らし方
          (L30) と検査の双方に反する
        category: skill
        audience: operator
        consequence:
          condition: 直前の回で捉え直しに合意して記録に書いたが、その単位の修正計画 (plans) をまだ書いていない状態で次のレビューを回し、捉え直しの軸の別のマスを指す採択が出たとき
          who: operator
          what: >-
            L39 に従った実行者は「修正計画が無いので修正由来の根拠は書けない」と読んで
            検知を書かず、L30 と検査が許す prior: 捉え直し の経路を使わない。俯瞰の連鎖が
            記録上で途切れ、捉え直しの後に同じ型が続いたことが残らない
          detectability: 気づかない — 文書内の矛盾を捕まえる機械検査は無く、検知を書かなかったことは記録に現れない
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: recurrence-detection.md:30 (捉え直し後は reframe.axes と照らし、prior には
          捉え直しを指す)、同 :38 (reframed なら捉え直しを指す文字列)、同 :39 (plans が無ければ
          修正由来の根拠は書けない、と無条件) を逐語で読み比べ、:39 が :30/:38 の経路を除外して
          いないことを確認した。record.go:715 の isReframe も plansByRun を見ないので、検査は
          この経路を通す。主張どおり。
          全 4 ゲートを評価して発火 0 件 — hypothetical: 状態は reframing.md の
          「記録に書いてから束ねに進む」の途中で普通に起きる。developer-domain: 対象は
          利用者に届くスキル文書。disproportionate-cost: 修正は 1 文に例外を添えるだけ。
          already-visible: 文書内の矛盾を検出する関門は無い。免除条項は対象外
        attrs:
          severity: P2
          confidence: high
    plans:
      - problem_id: P1
        cause: >-
          回 1 の P1 で「直前の回に plans が無いとき」の文を足したとき、直前の回の状態
          (通常 / reframed / declined / plans 無し) を軸にした表として他の文と突き合わせず、
          主の条件 2 (L21) と捉え直し後の照らし方 (L30) との整合を見なかった。同じ規則
          (修正由来の根拠が成り立つ条件・prior の形) が 3 つの文に散っていたので、1 文を
          足すと他の文との矛盾が生まれた
        finding_ids: [1]
        approach: >-
          例外を 1 文足すのではなく、「直前の回の状態 × 規則」の表を recurrence-detection.md
          に 1 つ置き、主の条件・捉え直し後・発火したときに書くものの各文をその表への参照にする。
          列は 通常 (plans あり) / reframed / declined / plans 無し、行は 主の条件 1 の照らし先 /
          条件 2 の要否 / prior に書くもの。reframed の列では条件 2 を不要にする (捉え直しは
          人間が繰り返しを認めた結果なので、今回の採択が表の別のマスを指すかだけで足りる)。
          あわせて、reframed で plans の無い回を prior: 捉え直し で通す実装済みの挙動の
          回帰テストを record_test.go に足す。順序: 問題は 1 つ
        investigation:
          scope: >-
            前向き: recurrence-detection.md の主の条件 (L14-22)・捉え直し後 (L28-32)・
            発火したときに記録に書くもの (L34-45) を全文読み直し、grep -n '修正由来の根拠\|plans\|
            修正計画' を recurrence-detection.md・record-schema.md・reframing.md に当てた。
            後ろ向き: record.go:713-718 の照合 (isReframe は plansByRun を見ない) と
            record_test.go の recurrenceReframedThenRecordYAML (reframed の回に plans がある
            fixture)、reframing.md:79「記録に書いてから束ねに進む」(reframed かつ plans 未記入を
            正当な途中状態として生む文)、record-schema.md:120 の prior の行、reframing.md:19 の辺
          included:
            - "recurrence-detection.md L21 主の条件 2 — reframed の後の扱いが未定義 (直前の回の検知が同じ場所の条件だけで発火して捉え直しに至った場合、条件 2 を満たせず L30 の経路が使えない)。同じ原因の別の現れ"
            - "record_test.go — reframed で plans の無い回を prior: 捉え直し で通すことを固定するテスト (レビュアのプローブと同じ形)。実装済みの挙動の回帰テスト"
          excluded:
            - "record.go:715 の isReframe — plans を見ない現状が文書の意図と一致するので変えない"
            - "record-schema.md:120・reframing.md:19 — 既に『reframed なら捉え直し』と書いており追随不要"
            - "reframing.md:79 — reframed かつ plans 未記入を途中状態として生む文だが、それ自体は正しい。L39 の例外がこの状態を同じ語で指すようにする"
        sha: 061f26b
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 cbca585..3e548d4) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。
      residual の 3 件はいずれも「委譲の宣言があり二重管理にならない」「設計どおりの非対称」
      という観察で、放置して壊れるものが無いため記録には写さない。
      このブランチで足した検知の手順を当てると、指摘 1 は回 1 の P1 の修正由来 (plans の
      無い場合の文を足したときに reframed の経路を除外しなかった) だが、回 1 には修正由来の
      指摘が無い (回 1 の指摘はレビュー前の作業に由来し、比べる前の回が無い) ので、主の条件の
      2 回連続には当たらない。補助の条件 (同じ場所への採択 2 回連続) は、回 1 の指摘 1 が
      record.go、回 2 の指摘 1 が recurrence-detection.md を指すので当たらない。発火しない
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: faa30bf
    findings:
      - id: 1
        file: review-triage/skills/review-triage/references/recurrence-detection.md
        line: 26
        summary: >-
          回 2 の修正で足した表の「通常」の行が直前の回の recurrence を「無いか declined」に
          限定したため、同じ文書が正常と明言する「直前の回が detected のまま未処理で plans が
          ある」状態がどの行にも当たらない。表を正本にした各文が、その状態で照らし先も
          条件 2 の要否も prior も定めない
        category: skill
        audience: operator
        consequence:
          condition: 直前の回で検知が出たが人間が review-triage-fix を呼ばず (または呼んで俯瞰を保留したまま)、別途 plans が書かれた状態で次のレビューを回し、今回の採択が直前の修正作業に由来するとき
          who: operator
          what: >-
            表に当たる行が無く、実行者は「捉え直し済み」でも「修正計画が無い」でもない状態を
            どう扱うか決められない。字面どおり「修正計画が無い」行に寄せると、plans が
            あるのに発火しない誤読になる。検知が記録に残らず、連鎖が途切れる
          detectability: 気づかない — 文書内の網羅の抜けを捕まえる機械検査は無く、発火しなかったことは記録に現れない
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: recurrence-detection.md:26 の表の「通常」の行 (recurrence は無いか
          declined) と、同 :63 の「直前の回が detected のまま未処理でも、判断を止めず通常
          どおり行う」を逐語で読み比べ、detected かつ plans がある状態が表のどの行にも
          当たらないことを確認した。主張どおり。
          全 4 ゲートを評価して発火 0 件 — hypothetical: 状態は運用で普通に起きる
          (人間が review-triage-fix を呼ばずにレビューを回す)。developer-domain: 対象は
          利用者に届くスキル文書。disproportionate-cost: 修正は表の行 1 つの条件の書き換え。
          already-visible: 表の網羅を検査する関門は無い。免除条項は対象外
        attrs:
          severity: P2
          confidence: high
    recurrence:
      status: reframed
      evidence:
        - condition: fix-derived
          finding_id: 1
          prior_run: 2
          prior: P1
          reason: >-
            回 2 の P1 で「直前の回の状態 × 規則」の表を足したとき、直前の回の状態の
            列挙から detected (未処理) を落とし、L63 の文と食い違った。回 2 の指摘 1 も
            回 1 の P1 (plans が無い場合の文を足したときに reframed の経路を除外しなかった)
            の修正由来で、修正由来の指摘が回 2 と回 3 で連続した。同じ場所の条件は、
            回 2 が「発火したときに記録に書くもの」の節、回 3 が「主の条件」の表なので
            当たらない
      reframe:
        pattern: 直前の回の状態の列挙が、状態 × 修正計画の有無の組み合わせを網羅していない
        axes: recurrence.status (無し / detected / declined / reframed) × plans の有無
        root_cause: >-
          表の行を「状態の列挙」で書いているため、状態を 1 つ考慮するたびにマスを 1 つ
          埋める修正になり、埋めていないマス (detected かつ plans あり) が残る
        fix_unit: >-
          行の条件を列挙ではなく順に問う 2 つの問い (reframed か → 捉え直し済み、
          そうでなければ plans があるか → 通常 / 修正計画が無い) で定め、どの状態でも
          必ずどれかの行に落ちるようにする
        source: skill
    plans:
      - problem_id: P1
        cause: >-
          回 2 の P1 で「直前の回の状態 × 規則」の表を足したとき、行の条件を状態の列挙で
          書き、recurrence.status × plans の有無の組み合わせを網羅しなかった (detected かつ
          plans あり が抜けた)。回 1・回 2 と同じく、状態を 1 つ考慮するたびにマスを 1 つ
          埋める修正になっていた
        finding_ids: [1]
        approach: >-
          捉え直しの単位のとおり、表の行の条件を順に問う 2 つの問いで定め直す。表の下に
          「通常の行には未処理の検知 (detected) も含む」と添えて「回ごとに判断する」の文と
          結びつける。検査とテストは変えない (照らし先の選択は検査の対象外)。問題は 1 つ
        investigation:
          scope: >-
            前向き: recurrence-detection.md の表 (L24-28)・捉え直し後の節・回ごとに判断する
            (L61-64) を全文読み直し、recurrence.status × plans の有無の 8 マスを表に書き出した。
            reframing.md「対象の選び方」と record-schema.md の状態の行 (状態を列挙する箇所) を
            目で読んだ。後ろ向き: record.go:713-718 (照らし先の選択を検査は行わない)、
            record_test.go の recurrence のテスト
          included:
            - "recurrence-detection.md の表の下の注記 — 通常の行が detected を含むことを L63 と結びつける"
          excluded:
            - "捉え直し後の節の「declined なら通常の行」 — 新しい定義でも成り立つので変えない"
            - "reframing.md「対象の選び方」・record-schema.md の状態の行 — 状態の定義そのものの列挙で、同じ原因ではない"
            - "record.go・record_test.go — 照らし先の選択は検査の対象外で変更不要。residual のテストのコメントの位置ずれは別の整理として残す"
        sha: 061f26b
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 3e548d4..faa30bf) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。
      residual の 3 件は整理の範疇か確認済みの事項で、放置して壊れるものが無いため記録には
      写さない。ただし 1 件目 (新テストの挿入で元のテストのコメントが移った) は、
      修正で record_test.go に触るなら同じ動機で直してよい。
      検知はこのブランチで足した手順 (recurrence-detection.md) をこのセッションで当てた
      もので、インストール済みのスキルの版には無い手順である。主の条件 (修正由来の指摘が
      2 回連続) で発火した。俯瞰は review-triage-fix の冒頭が担う
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: 80f8609
    findings: []
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 faa30bf..80f8609) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。指摘 0 件。
      residual のうち 1 件を写す: recurrence-detection.md:47 の「declined なら表の通常の行の
      とおり修正作業と照らす」は、その回に plans があるときだけ成り立つ (無ければ問い 2 で
      「修正計画が無い」の行に落ちる)。表が正本で L47 はそれを引くだけなので行の選択は
      狂わないが、文としては条件を省いている。放置して壊れるものは無い。
      採択が無いので検知の判断は行わない (対象は今回の採択だけ)。回 3 の捉え直しの後の
      増分で同じ型の指摘は出なかった
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: full
    head: 068efa8
    findings:
      - id: 1
        file: review-triage/skills/review-triage/references/recurrence-detection.md
        line: 9
        summary: >-
          検知の正本は「比べる相手は直前の 1 回」と定めるが、検査は prior_run を
          「1 以上かつ自回より小さい」としか見ないため、直前を飛び越して古い回を指す
          根拠が検査を通り、サマリと俯瞰の第 1 段の図にそのまま出る
        category: skill
        audience: operator
        consequence:
          condition: >-
            書き手が「繰り返しの起点はもっと前の回だ」と読んで、直前でない回を prior_run に
            書いたとき (たとえば直前の回の検知が declined で、その前の回を指す)
          who: operator
          what: >-
            検査は緑のまま通り、reframing.md の第 1 段の図は prior_run の prior から今回の
            指摘へ辺を引くので、途中の回を飛ばした連鎖が描かれる。人間は途中の回を見ない
            まま「繰り返していると認めますか」に答える
          detectability: 気づかない — 検査は緑で、サマリも図も飛び越しをそのまま描く
        premise_check:
          stages: A+B
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: record.go の priorRunOK (ev.PriorRun >= 1 && ev.PriorRun < runNo) を
          読み、上限が自回-1 で下限が 1 なので直前でない回も通ることを確認した。段 B:
          recurrence-detection.md:9「比べる相手は直前の 1 回」と record-schema.md の
          prior_run の行「1 以上かつ自回より小さいこと」を読み比べ、様式の表は検査と同じ
          広い範囲を書いていて、判断の正本 (直前の 1 回) だけが狭いことを確認した。
          回 2 の residual に同じ非対称を「どちらを規範にするか」と残していたが、今回は
          飛び越した辺が俯瞰の図に出るという具体の帰結が示されたので指摘として採る。
          免除条項の条件 1 (検査が正本の規則を検出しない) は成り立つが、関門の一覧
          (設定の gates) が未設定なので免除条項は使わない。全 4 ゲートを評価して発火
          0 件 — hypothetical: レビュアのプローブがそのままテストになる。developer-domain:
          検査は利用者の記録にかかる。disproportionate-cost: 直すのは検査の条件 1 行と
          テスト 1 ケースと様式の表の 1 行。already-visible: prior_run が直前でない回を
          指すケースのテストは無い (TestReviewTriageRecordRecurrenceViolations は 0 と
          自回以上だけ)。
          規範は判断の正本 (直前の 1 回。計画の KTD4 で人間が選んだ) に合わせ、検査と
          様式の表をそれに寄せる
        attrs:
          severity: P2
          confidence: high
    plans:
      - problem_id: P1
        cause: >-
          検査を書いたとき、判断の正本の prior_run の規則 (比べる相手は直前の 1 回・回 1 では
          判断しない・捉え直し済みの回は prior に 捉え直し と書く) を直接写さず、「1 以上かつ
          自回より小さい」という広い範囲で代用した。様式の表と README もその広い範囲を写した
        finding_ids: [1]
        approach: >-
          検査を正本の規則そのものに合わせる。prior_run は直前の回 (自回 - 1) に限り、回 1 の
          recurrence は「比べる過去の回が無い」と専用の文で弾き、比べた回が reframed なら
          fix-derived の prior は 捉え直し に限る (人間が含めると決めた)。様式の表の prior_run の
          行と README の検査項目の説明を同じ精度にそろえ、テストを足す。問題は 1 つ
        investigation:
          scope: >-
            前向き: recurrence-detection.md の prior_run に関わる規則 (L9・L10・表の捉え直し
            済みの行) と、record.go の priorRunOK・isReframe・plansByRun の照合、
            record-schema.md:119 の prior_run の行、triagecheck/README.md:11 の検査項目の説明を
            読み比べた。後ろ向き: record_test.go の recurrence のテスト全件 (prior_run は
            すべて直前の回を指す)、docs/review-triage/*.yaml の prior_run (この記録の回 3 の
            prior_run: 2 のみ、直前)、reframing.md:27 (根拠の外の辺を足してよい、という文)、
            計画 docs/plans/…-plan.md の U1 の検査の記述
          included:
            - "record-schema.md:119 の prior_run の行 — 広い範囲を写しているので直前の回に直す"
            - "record.go の回 1 の扱い — 範囲の副作用で弾いていた形を、正本どおり専用の文で弾く"
            - "record.go の捉え直し済みの回の prior — 正本は 捉え直し に限るが検査は問題の識別子も通していた。人間が含めると決めた"
            - "triagecheck/README.md:11 — 「過去の回」を「直前の回」に"
          excluded:
            - "reframing.md:27 — 根拠の外の辺を足す文は、根拠が直前を指す規則と整合するので変えない"
            - "docs/plans/…-plan.md の U1 — 当時の計画の記録なので直さない"
            - "既存のテストの fixture と実記録 — すべて直前の回を指しており、絞っても落ちない"
        sha: 020d835
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 最終確認の全量 main..068efa8) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。
      residual の 3 件は、根拠の重複 (図の辺が二重になるだけ)、捉え直しと修正計画の
      単位の一致は検査の対象外 (計画の KTD9 どおり)、reframing.md の対象の選び方の冗長な
      条件 (回 1 の観察に既出) で、いずれも放置して壊れるものが無いため記録には写さない。
      検知の判断: 直前の回 (回 4) は指摘 0 件で修正計画が無く、表の「修正計画が無い」の
      行に当たるので修正由来の根拠は書けない。回 4 に採択が無いので同じ場所の条件も
      当たらない。発火しない
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: 7ca7e36
    findings:
      - id: 1
        file: review-triage/skills/review-triage/references/record-schema.md
        line: 120
        summary: >-
          回 5 の修正は prior_run の行だけを正本の精度にそろえ、隣の prior の行を
          「問題の識別子か、比べた回が reframed のときの 捉え直し」の選べる形のまま残した。
          検査は同じ修正で「捉え直し済みの回なら 捉え直し に限る」へ狭めたので、様式の表を
          正本として書いた記録が検査に落ちる
        category: skill
        audience: operator
        consequence:
          condition: 書き手が様式の表の prior の行を読み、捉え直し済みの回を比べた先にした修正由来の根拠に問題の識別子を書いたとき
          who: operator
          what: >-
            検査が赤くなり、様式の表どおりに書いた記録が通らない。判断の正本 (表の捉え直し
            済みの行) と様式の表のどちらが正しいかを読み手が判別できない
          detectability: 検査が赤くなるので落ちたことには気づく。ただし、様式の表と検査のどちらを信じるべきかは気づけない
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: record-schema.md:120 の「問題の識別子 (例: P6) か、比べた回が reframed
          のときの 捉え直し」と、record.go:725-728 の「比べた回が捉え直し済みなら prior は
          捉え直し と書く」を読み比べ、様式の表が選べる形のまま残っていることを確認した。
          判断の正本 (recurrence-detection.md の表の捉え直し済みの行) は 捉え直し だけを
          書いており、検査はそれと一致する。主張どおり。
          全 4 ゲートを評価して発火 0 件 — hypothetical: レビュアのプローブがそのまま
          テストになる。developer-domain: 対象は利用者に届く様式の表。disproportionate-cost:
          修正は表の 1 行の文言。already-visible: 様式の表と検査の食い違いを検出する
          関門は無い。免除条項は対象外
        attrs:
          severity: P2
          confidence: high
      - id: 2
        file: review-triage/tools/triagecheck/record_test.go
        line: 1419
        summary: >-
          回 5 で足したケース「prior_run が直前でない回を指す」の入力が、直上の「prior_run が
          0」と同一で、名前が言う「直前でない回」を通していない。検査の狭めが緩んで
          prior_run < runNo に戻っても、このケースは 0 が範囲外であることだけで通り続ける
        category: test
        audience: developer
        consequence:
          condition: 将来、検査の条件が「直前の回に限る」から「自回より小さい」に緩められたとき
          who: developer
          what: >-
            名前の上では直前でない回を守っているはずのケースが落ちず、緩んだことに
            このケースでは気づけない (別のテスト TestReviewTriageRecordRecurrencePriorRunNotPrevious
            が同じ分岐を踏んでいるので、検査そのものの穴にはならない)
          detectability: 気づかない — テストが主張どおりの入力を通しているかを検査する関門は無い
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: record_test.go:1417-1420 を読み、2 つのケースの recurrence を作る式が
          同一で、違うのは期待する語だけであることを確認した。主張どおり。
          全 4 ゲートを評価して発火 0 件 — hypothetical: 入力を変えれば再現する。
          developer-domain: 環境の異常ではない。disproportionate-cost: 修正は 1 ケースの
          入力の差し替えか削除。already-visible: テストが名前どおりの入力を通しているかを
          見る関門は無い。免除条項は対象外。被害者は開発者だがゲートが発火していないので採択
        attrs:
          severity: P3
          confidence: high
      - id: 3
        file: review-triage/tools/triagecheck/README.md
        line: 11
        summary: >-
          検査項目の説明を「修正由来の根拠の prior が比べた回の修正計画か捉え直しを指すこと」と
          書いたが、同じ修正で検査は「捉え直し済みの回なら 捉え直し に限る」へ狭めた。
          README も指摘 1 と同じ選べる形のまま残っている
        category: doc-user
        audience: operator
        consequence:
          condition: 利用者が README の検査項目の説明から、捉え直し済みの回でも問題の識別子を書けると読んだとき
          who: operator
          what: 指摘 1 と同じ — 検査が赤くなり、説明と検査のどちらが正しいか判別できない
          detectability: 検査が赤くなるので落ちたことには気づく。説明が古いことには気づけない
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: README.md:11 の「修正計画か捉え直しを指すこと」を読み、選べる形のまま
          であることを確認した。指摘 1 と原因が同じ (prior_run の側だけ直して prior の側の
          文言を残した)。全 4 ゲートを評価して発火 0 件 — 内容は指摘 1 と同じ。免除条項は対象外
        attrs:
          severity: P3
          confidence: high
    plans:
      - problem_id: P1
        cause: >-
          回 5 で検査を「捉え直し済みの回なら 捉え直し に限る」へ狭めたとき、同じ規則を
          写している並び (様式の表の隣の行・README の説明・検査の報告文・構造体とテストの
          コメント・足したテストの入力) を読み直さず、検査の分岐だけを直した。回 1 の P1
          (検査を足したとき文書を直さなかった) と同じ型
        finding_ids: [1, 2, 3]
        approach: >-
          規則を写している文をすべて、正本 (recurrence-detection.md の表) と同じ「状態で
          書き分ける形」にそろえる。record-schema.md の prior の行、README の検査項目の
          説明、record.go の報告文 (捉え直し済みでない回の分岐なので「問題の識別子」だけ) と
          構造体のコメント、record_test.go の古いコメント。指摘 2 の重複した行は、2 回の
          記録では「直前でない回」を作れないので削り、3 回の記録で踏む
          TestReviewTriageRecordRecurrencePriorRunNotPrevious に任せる。問題は 1 つ。
          直した後に「問題の識別子」を含む文を grep し直し、状態で書き分けた形か参照つきの
          要約だけが残ることを確かめる (観点 B)
        investigation:
          scope: >-
            前向き: grep -rn '問題の識別子' を CONCEPTS.md・review-triage/README.md・
            review-triage/skills・triagecheck の README・record.go・record_test.go に当て、
            選べる形の文言を列挙した。後ろ向き: record.go の報告文の分岐 (!priorReframed の
            ときだけ届く)、既存のテストの期待する語 (want は部分一致)、実記録の根拠
          included:
            - "record.go:730 の報告文 — 捉え直し済みでない回にしか届かないので「問題の識別子」だけにする (回 6 の residual)"
            - "record.go:101 の構造体のコメント — 条件で形が決まることを書く"
            - "record_test.go:1516 の古いコメント — 回 2 でテストを挿入したとき元のテストから離れた (回 2 の residual)。状態で書き分けた形に直して元のテストへ戻す"
          excluded:
            - "reframing.md:19 の辺の説明 — 書き分けの正本への参照つきの要約なので残す"
            - "recurrence-detection.md:53 — 表の prior の列の値を並べただけで、どちらを書くかは表の行が決める"
            - "record.go:715 のコメント — 状態ごとに書き分けた正しい形"
        sha: 00650d8
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 068efa8..7ca7e36) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。
      residual のうち 1 件を写す: record.go:730 の報告文も「問題の識別子か、reframed のときの
      捉え直し」という選べる形の文言で、捉え直し済みでない回にしか届かないので誤読の経路には
      ならないが、指摘 1・3 と同じ原因なので同じ問題で直してよい。回 1 の recurrence の
      早期 return で他の違反が報告されない件は、plans の status と同じ扱いで放置して壊れる
      ものが無い。
      検知の判断: 指摘 1〜3 はいずれも回 5 の P1 (検査を狭めた修正) の修正由来だが、回 5 の
      指摘は修正由来ではない (直前の回 4 に修正計画が無い) ので、主の条件の 2 回連続には
      当たらない。同じ場所の条件は、回 5 が recurrence-detection.md、回 6 が record-schema.md
      / record_test.go / README.md なので当たらない。発火しない。ただし、修正の隣を直し
      残す型は回 1 の P1 (検査を足したとき文書を直さなかった) と同じで、回 5 の修正でも
      「prior_run の行だけ直して prior の行を残す」形で再発している
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: c7a0d3b
    findings:
      - id: 1
        file: review-triage/tools/triagecheck/record.go
        line: 102
        summary: >-
          回 6 で書き換えた構造体のコメントだけが、規則を「問題の識別子か (比べた回が
          捉え直し済みなら) 捉え直し」という選べる形のまま残っている。同じ修正で直した
          record-schema.md と README は「なら…そうでなければ」の書き分けの形になっている
        category: plugin-code
        audience: operator
        consequence:
          condition: triagecheck を保守する開発者が Prior フィールドのコメントを読み、両方の形が許されると読んで検査を緩めたり報告文を書き換えたりしたとき
          who: developer
          what: >-
            検査が正本より広くなる。ただし緩めれば TestReviewTriageRecordRecurrencePriorReframedRunRequiresReframeMarker
            が落ちる。コメントを読むのは開発者だけで、記録を書く利用者はこの文を読まない
          detectability: 気づく — 緩めればテストが落ちる。コメントの古い形そのものは機械検査の対象外
        premise_check:
          stages: A
          result: verified
        gates_fired: [already-visible]
        verdict: adopted
        verdict_reason: >-
          H2。段 A: record.go:101-103 を読み、括弧で条件を添えつつ「識別子か…捉え直し」と
          並べる形であることを確認した。主張どおり。
          被害者: パスの分類 (plugin-code) では利用者だが、内容はコードのコメントで、
          読むのはツールを保守する開発者だけ。利用者 → 開発者の向きの上書きは機械的には
          行わず、初期値のまま確定もしない規則なので、D2 で決められないとして保留。
          全 4 ゲートを評価した (記録に残すため) — hypothetical: コメントの読み違いは
          テストで再現できないが、読み違いの結果 (検査を緩める) はテストで捕まる。
          developer-domain: 環境の異常ではないので当たらない。disproportionate-cost:
          修正は 1 行の文言。already-visible: 発火 — 緩めれば
          TestReviewTriageRecordRecurrencePriorReframedRunRequiresReframeMarker が落ちる。
          免除条項は対象外。
          人間が被害者を開発者と確定すれば R3 (却下)、利用者と確定すれば H6 相当で
          人間の判断、採択して 1 行直すのも可。→ 人間が保留を読んで採択に変えた
          (図で 3 案を比べ、写しの数そのものを減らす案 (c) を選んだ)
        attrs:
          severity: P3
          confidence: high
    plans:
      - problem_id: P1
        cause: >-
          規則 (prior の書き分け) の正本は 1 か所だが、それを言い直した写しがコードの
          コメントと報告文にもあり、規則を変えるたびに写しの並びを読み直す必要があった。
          回 1・回 5・回 6 と同じ型 (規則を変えたとき写しを直し残す) で、写しが多いこと
          そのものが再発の構造
        finding_ids: [1]
        approach: >-
          人間が選んだ案 (c)。コメントと報告文は規則を言い直さず、正本 (recurrence-detection.md
          の表) への参照だけにする。構造体のコメント (record.go:101-103) は「形の正本は
          recurrence-detection.md」とだけ書く。報告文 (record.go:730・:732) は違反した条件を
          名指しするが、規則の全体は言い直さず正本を指す。様式の表と README の写しは、記録を
          書く利用者が読む要約なので残す (正本への参照つき)。問題は 1 つ
        investigation:
          scope: >-
            前向き: grep -rn '問題の識別子' を review-triage/skills・triagecheck の README・
            record.go・record_test.go に当て、規則を言い直している箇所を列挙した (前回の
            調査と同じ範囲)。後ろ向き: 報告文の語を部分一致で確かめるテスト
            (TestReviewTriageRecordRecurrenceViolations の want、
            TestReviewTriageRecordRecurrencePriorReframedRunRequiresReframeMarker の
            「捉え直し」「\"P2\"」) と、報告文を読む利用者
          included:
            - "record.go:732 の報告文の括弧 — 規則の言い直しを正本への参照に置き換える"
          excluded:
            - "record-schema.md:120・README.md:11 — 利用者が読む要約で、正本への参照つき。残す"
            - "record.go:717 のコメント — 検査の意図 (なぜ照合するか) を書く文で、規則の言い直しではない"
            - "record_test.go:1604 のコメント — 表の行を名指しする参照"
        sha: c6a02ac
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 7ca7e36..c7a0d3b) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。
      residual の 3 件 (reframing.md:19 の「か」は読み手向けの要約、削ったテストの行の被覆は
      ミューテーションで確認済み、record.go:730 は対称) は放置して壊れるものが無いため
      記録には写さない。
      検知の判断: 対象は今回の採択だけで、今回は採択が無い (保留 1 件) ので発火しない。
      ただし指摘 1 は回 6 の P1 の修正由来で、回 6 の 3 件も回 5 の P1 の修正由来だった
      ので、人間が保留を採択に変えれば主の条件 (修正由来の指摘が 2 回連続) に当たる。
      保留が採択に変わったときに検知をやり直す手順は recurrence-detection.md に無い —
      設計の抜けとして残す。人間が採択に変えた時点で当てると、主の条件 (修正由来の指摘が
      2 回連続: 回 6 ← 回 5 の修正、回 7 ← 回 6 の修正) に当たる。俯瞰は図で行い、型を
      「規則を変えたとき写しを直し残す」、修正の単位を「写しの数を減らす」と捉え直した
      (案 (c))。検知の項目は書かない — 保留から採択への変更は判定の後で、review-triage の
      手順の外で起きたため。この手順の抜けは別途直す
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: 59eed18
    findings:
      - id: 1
        file: review-triage/tools/triagecheck/record.go
        line: 727
        summary: >-
          回 7 の修正は record.go から規則の言い直しを取り除いたが、捉え直し済みの行の分岐に
          付いたコメントだけが「prior は 捉え直し に限る」と規則を言い直したまま残っている。
          同じ switch を囲むコメントは「ここでは言い直さない」と宣言しており、宣言と中身が
          食い違う。回 7 の修正計画の excluded にもこの箇所は無い
        category: plugin-code
        audience: operator
        consequence:
          condition: 正本の表の捉え直し済みの行の prior 列を変えたとき (印の文字列を変える、または捉え直し済みの回でも問題の識別子を許す)
          who: developer
          what: >-
            case 式と報告文は目に入るが、その間のコメントは「捉え直し に限る」と旧規則を
            主張したまま残る。回 7 の修正が断とうとした型 (規則を変えたとき写しを直し残す) が
            この 1 か所で再発する
          detectability: 気づかない — 古いコメントを捕まえる機械検査は無い
        premise_check:
          stages: A
          result: verified
        gates_fired: []
        verdict: adopted
        verdict_reason: >-
          A2。段 A: record.go:716-718 の宣言 (ここでは言い直さず、表どおりかを照合する) と
          :727-728 の「prior は 捉え直し に限る」を読み比べ、後者が正本の表の捉え直し済みの
          行の prior 列の言い直しであることを確認した。主張どおり。
          被害者: 回 7 と同じくコードのコメントで読むのは開発者だが、回 7 で人間が
          「規則を言い直したコメントは写しの数を減らす形で直す」と決めており、この指摘は
          その修正 (回 7 の P1) の取り残しなので、同じ扱いで採択する。
          全 4 ゲートを評価して発火 0 件 — hypothetical: 正本を変えれば再現する。
          developer-domain: 環境の異常ではない。disproportionate-cost: 修正はコメント 1 行。
          already-visible: 古いコメントを捕まえる関門は無い (回 7 では「緩めればテストが
          落ちる」が発火したが、今回の帰結は検査の緩みではなくコメントの取り残し)。
          免除条項は対象外
        attrs:
          severity: P3
          confidence: high
    recurrence:
      status: reframed
      evidence:
        - condition: fix-derived
          finding_id: 1
          prior_run: 7
          prior: P1
          reason: >-
            回 7 の P1 (コメントと報告文の言い直しを正本への参照に置き換えた) で、同じ switch
            の中のコメント 1 つを直し残した。回 7 の指摘 1 も回 6 の P1 (写しを書き分けの形に
            そろえた) の修正由来だったので、修正由来の指摘が回 7 と回 8 で連続した。補助の条件は
            当たらない — 回 7 の指摘は record.go:102 の構造体の宣言、回 8 は :727 の照合の
            関数で、同じファイルだが同じ関数ではない
      reframe:
        pattern: 規則を変えたとき、写しを列挙する基準が旧規則の語なので、語の違う写しを直し残す
        axes: 写しの場所 (様式の表 / README / 報告文 / 構造体のコメント / 照合の意図のコメント / 分岐ごとのコメント / テストのコメント) × 写しを列挙する基準 (旧規則の語で探す / 新しく書いた宣言に照らしてその範囲を読む)
        root_cause: >-
          回 6・回 7 の修正は、旧規則の語 (問題の識別子) で grep して写しを列挙し、語の違う
          写し (「捉え直し に限る」) は列挙の外に落ちた。「言い直さない」という宣言を書いた
          あとで、その宣言が及ぶ範囲 (同じ switch の中・同じ関数・同じ構造体) を宣言に
          照らして読み直す手順が無かった。回 8 の指摘を見つけたレビューはその読み方をしている
        fix_unit: >-
          宣言 (record.go:101-103 と :716-718) が及ぶ範囲の全文を宣言に照らして読み、
          規則を言い直したコメントと報告文をすべて正本への参照に置き換える。
          旧規則の語で grep した結果を修正の範囲にしない
        source: human
    plans:
      - problem_id: P1
        cause: >-
          捉え直しの root_cause のとおり。回 7 の P1 は旧規則の語 (問題の識別子) で grep して
          写しを列挙したので、語の違う写し (record.go:727-728 の「捉え直し に限る」) が
          列挙から落ち、宣言 (言い直さない) の及ぶ範囲を宣言に照らして読み直していなかった
        finding_ids: [1]
        approach: >-
          捉え直しの fix_unit のとおり。宣言が及ぶ範囲 (record.go:101-103 の構造体のコメント
          が覆う構造体の並びと、:716-718 が覆う照合の関数 recordRecurrenceProblems の全文) を
          宣言に照らして読み、規則を言い直したコメントを外す。:727-728 の 2 行は、囲む宣言が
          既に照合の意図を書いているので置き換えず削る (もう一方の分岐にもコメントは無く、
          対称になる)。報告文は違反した条件を名指しする形 (回 7 で決定) のまま
        investigation:
          scope: >-
            前向き: grep ではなく、2 つの宣言が及ぶ範囲を全文読んだ (record.go:78-120 と
            :664-780)。あわせて record.go の全コメントから 捉え直し / prior / 識別子 / 正本 /
            言い直 を含む行を列挙し、宣言の外に言い直しが無いことを確かめた。
            後ろ向き: go test ./... と実記録の検査
          included: []
          excluded:
            - "record_test.go:1467・:1514-1516・:1603-1604・:1626-1627 のコメント — テストが固定する挙動を表の行の名指しで書いた参照で、宣言 (record.go) の範囲の外。回 7 の excluded と同じ判断"
            - "record.go:729・:732 の報告文 — 違反した条件を名指しする形は回 7 の決定どおり"
        sha: 31df573
        status: done
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 c7a0d3b..59eed18) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。
      residual の 2 件 (報告文の簡素化は設計どおり、検知のやり直しの抜けは既に notes に
      ある) は記録には写さない。
      検知はこのブランチで足した手順を当てたもので、主の条件 (修正由来の指摘が 2 回連続) で
      発火した。回 3 に続いて 2 度目。型は回 1 から続く「規則を変えたとき写しを直し残す」で、
      回 7 で「写しの数を減らす」と捉え直したが、その修正自身が写しを 1 つ取り残した。
      俯瞰は review-triage-fix の冒頭が担う
  - date: "2026-09-04"
    skill: code-review
    run_id: ""
    model: opus-5
    level: high
    scope: incremental
    head: 0156d18
    findings: []
    notes: >-
      レビューは別セッションの code-review (opus-5, high, 増分 59eed18..0156d18) で、
      報告のみで走った (実行前後で HEAD と作業ツリーが不変であることを確認)。指摘 0 件。
      residual のうち 1 件を写す: 回 8 の記録の excluded が引く record.go:729・:732 は
      修正前の行番号で、修正後は 2 行ずれる。記録はその回のレビュー時点の HEAD の行を
      指すのが様式として一貫しているので齟齬ではない。放置して壊れるものは無い。
      採択が無いので検知の判断は行わない (対象は今回の採択だけ)。回 8 の捉え直し
      (宣言が及ぶ範囲を宣言に照らして読む) の後の増分で、同じ型の指摘は出なかった
